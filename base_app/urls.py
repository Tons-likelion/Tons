from django.urls import path
from . import views

urlpatterns = [
    # path('<int:blog_id>/', views.detail, name='detail'),
    # path('new/', views.new, name='new'),
    # path('create/', views.create, name='create'),
    # path('<int:cat_id>/', views.category_detail, name='cat_detail'),
]